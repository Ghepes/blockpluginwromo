/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

export default function save() {
	return (
		<p { ...useBlockProps.save() }>
			{ __( 'BlockPluginWromo – hello from the saved content!', 'blockpluginwromo' ) }
		</p>
	);
}

// Path: src\save.js
/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

export default function save() {
	return (
		<p { ...useBlockProps.save() }>
			{ __( 'BlockPluginWromo – hello from the saved content!', 'blockpluginwromo' ) }
		</p>
	);
}

// Path: src\save.js
/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

export default function save() {
	return (
		<p { ...useBlockProps.save() }>
			{ __( 'BlockPluginWromo – hello from the saved content!', 'blockpluginwromo' ) }
		</p>
	);
}

// Path: src\save.js
/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

export default function save() {
	return (
		<p { ...useBlockProps.save() }>
			{ __( 'BlockPluginWromo – hello from the saved content!', 'blockpluginwromo' ) }
		</p>
	);
}

// Path: src\save.js
/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

export default function save() {
	return (
		<p { ...useBlockProps.save() }>
			{ __( 'BlockPluginWromo – hello from the saved content!', 'blockpluginwromo' ) }
		</p>
	);
}

// Path: src\save.js
/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps } from '@wordpress/block-editor';

export default function save() {
	return (
		<p { ...useBlockProps.save() }>
			{ __( 'BlockPluginWromo – hello from the saved content!', 'blockpluginwromo' ) }

		</p>
	);
}

// Path: src\save.js





